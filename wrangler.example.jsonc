{
  "$schema": "./node_modules/wrangler/config-schema.json",
  "name": "fetch-bridge",
  "main": ".open-next/worker.js",
  "compatibility_date": "2026-07-23",
  "compatibility_flags": ["nodejs_compat"],
  "routes": [
    {
      "pattern": "fetch-bridge.example.com",
      "custom_domain": true,
    },
  ],
  "assets": {
    "directory": ".open-next/assets",
    "binding": "ASSETS",
  },
  "observability": {
    "enabled": true,
  },
  "vars": {
    // NextAuth 在 Workers 上需要的普通变量，随部署自动生效，无需在 Dashboard 手动添加
    "AUTH_TRUST_HOST": "true",
  },
  "d1_databases": [
    {
      "binding": "DB",
      "database_name": "fetch-bridge",
      "database_id": "REPLACE_WITH_D1_DATABASE_ID",
      "migrations_dir": "prisma/sqlite/migrations",
      "migrations_pattern": "prisma/sqlite/migrations/*/migration.sql",
    },
  ],
}
