# Components

组件按功能域归档，页面只从对应目录引用：

- `admin/`：`/console` 后台的 Sources、日志、设置及其子组件。
- `auth/`：登录与 Passkey 身份验证组件。
- `home/`：公开首页专用组件。
- `ui/`：跨页面复用的无业务语义 UI 基元。
- 根目录：真正跨功能域的展示组件（目前为主题切换）。

管理后台页面优先在服务端取数和渲染；新增组件应先放入所属功能目录，避免重新堆积在根目录。
